# Create a second stage that will be will be deployed

Tasks

- Copy over necessary paths
  - /usr/lib
  - /usr/share
  - /etc/fonts
  - /var/cache/fontconfig
  - /etc/passwd
  - /etc/group
  - /etc/shadow

Why are these files necessary?

- Copy the compiled application only
- Set the user and start the app as entrypoint
